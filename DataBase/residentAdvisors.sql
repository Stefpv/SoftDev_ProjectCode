CREATE TABLE IF NOT EXISTS staff_users(
    first_name TYPE, /* first name */
    last_name TYPE, /* last name */
    student_email TYPE, /* student email */
    user_password TYPE, /* password */
    campus_region TYPE, /* region on CU Boulder Campus*/
    residence_hall TYPE, /* residence hall */
    floor_number TYPE, /* floor number in given residence hall */
); 